apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-game-engine-logger-backend.fullname" . }}
  labels:
    {{- include "cloud-game-engine-logger-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-game-engine-logger-backend.selectorLabels" . | nindent 4 }}
